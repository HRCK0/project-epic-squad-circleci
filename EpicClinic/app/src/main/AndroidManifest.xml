<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uottawa.mali165.epicclinic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClinicDescriptionActivity"></activity>
        <activity android:name=".RatingsActivity"></activity>
        <activity android:name=".MakeRatingActivity"></activity>
        <activity android:name=".BookAppointmentActivity" />
        <activity
            android:name=".PatientServicesActivity"
            android:windowSoftInputMode="adjustPan"></activity>
        <activity android:name=".AvailabilityActivity" />
        <activity android:name=".CompleteUserProfileActivity" />
        <activity android:name=".ServicesActivityNonAdmin" />
        <activity android:name=".ServicesActivityNonAdmin_2" />
        <activity android:name=".ServicesActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" /> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login" /> -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>